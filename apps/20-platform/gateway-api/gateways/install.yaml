apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gateway-resource
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  project: platform-components
  source:
    repoURL: https://github.com/hogonzalezve/platform-infra.git
    targetRevision: main
    path: apps/20-platform/gateway-api/gateways/manifests
  destination:
    name: in-cluster
    namespace: infra-networking
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
